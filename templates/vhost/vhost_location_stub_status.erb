  location <%= location %> {
    <% if ( scope.function_is_hash(custom_config) ) -%>
      <% custom_config.each do |key,value| -%>
      <%= key %> = <%= value %>;
      <% end -%>
    <% end -%>
    stub_status on;
  }
